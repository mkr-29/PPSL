#include <stdio.h>
#include <stdlib.h>

int main()
{
    unsigned int w;  //choose a variable for the input of type unsigned int
    scanf("%d",&w); //take input from the user for the chosen variable
    printf("Number is %d",a); //print the required output
    return 0;
}
