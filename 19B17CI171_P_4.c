#include <stdio.h>
#include <stdlib.h>

int main()
{
    float f;  //choose a variable for type float
    scanf("%f",&a);   //take input from the user for the chosen variable
    printf("Number is %0.2f",a);    //print the desired output (%0.2f is is used to print the float upto 2 places of decimal)
    return 0;
}
